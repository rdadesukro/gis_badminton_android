<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projek_kp_gis_badminton_2021">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projek_KP_GIS_Badminton_2021">
        <activity android:name=".menu.menu_lapangan"></activity>
        <activity android:name=".menu.fragment.menu_profil" />
        <activity android:name=".menu.fragment.menu_home" />
        <activity android:name=".menu.menu_login" />
        <activity android:name=".menu.menu_utama" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>